[project]
name = "spark-medallion-architecture"
version = "0.1.0"

dependencies = [
    "pyspark>=4.0",
    "delta-spark",
    "pandas",
    "pyarrow",
    "python-dotenv",
    "pyyaml",
    "loguru",
]

[dependency-groups]
dev = [
    "black",
    "ruff",
    "pytest",
]
